// src/components/RadioGroupItem.jsx
//import React from 'react';

//const RadioGroupItem = ({ value, children, checked }) => {
  //return (
    //<div className={`flex items-center space-x-2 my-2 ${checked ? 'bg-blue-500' : ''}`}>
     // <input type="radio" value={value} checked={checked} readOnly />
   //   <span className={`${checked ? 'font-semibold' : ''}`}>{children}</span>
 //   </div>
  //);
//};

//export default RadioGroupItem;
